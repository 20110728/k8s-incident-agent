# 变更记录

## 未发布：恢复闭环与只读复查阶段存档（2026-09-15）

本条汇总阶段 1–5 快照之后的增量；“恢复阶段 1/2”是后续交付编号，不是重新实现旧的 v0.2 阶段 1/2。未调整包/API 版本，不宣称完整 v0.2.0 发布。

### 新增与修正

- 处置后重新采集资源和登记业务证据，保存 post_repair_evidence、profile、错误及 unverified_scope。
- service profile 增加可选 expected_replicas；订单 Demo 登记为 2。未设置该字段的历史 profile 保持摘要兼容。
- 将探针配置分为 matched/drift/unknown，区分已知无 HTTP 探针和证据缺失；自动修复能力另行判断，不把所有漂移都视为可修复。
- 退出旧 Pod 仅在严格匹配的 EndpointSlice 明确不就绪、不服务且正在退出时，从恢复判定视图排除；保留原始证据和最终删除未验证范围。
- context_builder 的动态 output_contract 说明当前输出要求；supported 配置假设缺少必需配置引用时，在诊断阶段返回校验反馈，使用已有重试，不自动补造假设依据。
- llm_debug 保存诊断/规划服务逐次调用的响应、解析、错误、重试反馈、耗时与实际用量；底层 SDK 自身重试不保证逐次可见。
- 新增人工处理后只读复查 API/CLI 和 PostgreSQL 独立记录。比较原/新目标的登记摘要、Deployment UID/generation；保留原事件，备注不作为事实，健康不归因为 Agent 修复。

### 本次可读性整理

- 重写 README，明确使用对象、架构、改动、校验职责、边界、验收层级与剩余步骤。
- 更新代码阅读、提交推送与存档说明，记录维护者最新验收反馈。
- 仅整理 diagnosis_policy、business_recovery、recheck service/repository/routes 五个文件的注释和格式；不修改函数签名、状态、提示词或执行逻辑，不新增运行依赖。

### 验收记录与限制

- 定向测试历史批次：恢复边界 183 项、配置引用修正相关 117 项、只读复查相关 50 项通过；有重叠，不能累计为独立测试总量。
- 维护者反馈 selector/readiness 真实修复及 Recovery verification 通过；随后反馈人工调查事件复查和重启后记录保留均通过。
- 本次仅做 Python AST 等价、Markdown 本地链接和补丁应用/重复应用检查，不重复已通过且未受影响的集群测试。
- Web 复查交互、延时/连续业务验证、真实受限身份验收、完整并发边界和规则/Agent 实测指标尚未完成；不据单次模型成功宣称稳定。

## 历史记录：v0.2 阶段 1–5

基于已验收的 Day21 版本增量开发。以下为当时记录；后续能力与验收状态以上方最新记录为准。

### 新增

- 版本化服务配置及执行前现场一致性复核。
- 简单订单 HTTP 应用、独立模拟下游和可重复故障模式。
- 集群内固定目标业务检查，结果进入 Evidence。
- v2 诊断结构：问题领域、症状、根因假设、缺失证据、调查步骤、资源/业务状态与未验证范围。
- 十个固定案例、七个真实事实场景、独立回放输入与结果记录。

### 修正

- Ready 与业务通过分开判断，HTTP 200 需要继续验证内容。
- 当前异常与历史事件分开，连接错误不直接确认为应用或依赖根因。
- 模型遗漏的引用仅在确属已有有效 Evidence 时补入总索引；不存在的引用仍拒绝。
- 特定证据不足事实组合在调用诊断模型前生成保守报告，明确来源为 `rule_precheck`。
- 已校验的业务失败分类采用程序构建的正式说明，并另存模型原始结构，减少无证据扩展根因的表述。
- 写操作以登记配置和当前证据为门槛，审批后应用或配置变化时返回冲突。

### 文档与可读性（2026-09-12）

- 新建根 README，说明更新、运行方式、模式差异、固定案例和验收范围。
- 为 22 个核心 Python 文件补充中文开头说明与关键行间注释；仅改注释，语法树保持一致。
- 新增代码阅读指南与 ECS 提交/推送步骤；忽略本地案例运行结果目录。

### 验证范围

维护者反馈阶段 5 固定回归和真实事实批测全部通过。此记录不是全部历史测试、全部真实模型场景或完整 v0.2 产品验收。处置后业务恢复验证、真实受限身份验收和规则/Agent 指标对比仍待后续阶段实施。
