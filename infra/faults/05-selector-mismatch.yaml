apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: agent-demo
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: wrong-service
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP